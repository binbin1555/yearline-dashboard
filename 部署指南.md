# 部署指南（零基础版）

全程约 30 分钟。按顺序做，不要跳步。
遇到界面文字对不上，按**功能**找，各家网站会改版。

---

## ⚠️ 先看这条

在 GitHub Desktop 里移除仓库时，弹窗中有一个勾选项，字面意思类似
**"Also move this repository to Trash"**（同时把仓库移到回收站）。

**千万不要勾。** 勾了会把 `F:\投资\创业板+红利轮动\repo` 整个文件夹删掉，而且不一定能从回收站找回。

如果只是想让它从 GitHub Desktop 的列表里消失，**保持那个勾选框是空的**再确认。

---

## 第 1 步 · 把代码发布到 GitHub

本地的 git 仓库我已经建好并提交过了，你只需要发布。

1. 打开 **GitHub Desktop**
2. 菜单栏 `File` → `Add local repository...`
3. 点 `Choose...`，选中文件夹：
   ```
   F:\投资\创业板+红利轮动\repo
   ```
4. 点 `Add repository`
5. 窗口上方会出现蓝色按钮 **`Publish repository`**，点它
6. 弹窗里：
   - **Name**：`yearline-rotation`（想换别的名字也行，只能用英文、数字、连字符）
   - **Description**：随便填，可留空
   - **⚠️ 把 `Keep this code private` 的勾去掉** —— 必须公开，否则网页功能要付费
7. 点 `Publish repository`

完成后你的仓库地址是：
```
https://github.com/你的用户名/yearline-rotation
```

> **记下你的 GitHub 用户名**，后面每一步都要用。它就在上面这个网址里。

---

## 第 2 步 · 开启网页

1. 浏览器打开你的仓库页面
2. 点上方的 **`Settings`**（齿轮图标那一行，注意是**仓库**的 Settings，不是账号的）
3. 左侧栏找到 **`Pages`**
4. `Build and deployment` 下面：
   - **Source**：选 `Deploy from a branch`
   - **Branch**：左边选 `main`，右边选 `/ (root)`
5. 点 `Save`

等 2～5 分钟，刷新页面，顶部会显示你的网址：
```
https://你的用户名.github.io/yearline-rotation/
```

打开它，应该能看到看板。**把这个网址存进手机浏览器书签。**

---

## 第 3 步 · 拿到 Bark 密钥

Bark 是一个 iOS 推送 App（安卓用不了，安卓请看本文末尾）。

1. App Store 搜索 **Bark**，下载安装（免费）
2. 打开 App，首页会显示一串网址，长这样：
   ```
   https://api.day.app/abcDEF123456xyz/
   ```
3. 中间那段 **`abcDEF123456xyz`** 就是你的密钥（每个人不一样，通常 20 位左右）
4. App 里有复制按钮，点一下复制整串网址，然后**只保留中间那段**

> 别把整串网址当密钥。只要 `api.day.app/` 后面、最后一个 `/` 前面的那段。
>
> **这个密钥不要发给任何人，包括我。** 拿到它的人可以给你手机推任意消息。

---

## 第 4 步 · 把密钥存进 GitHub

1. 回到你的仓库页面 → **`Settings`**
2. 左侧栏 → **`Secrets and variables`** → 点开后选 **`Actions`**
3. 右上角绿色按钮 **`New repository secret`**
4. 填：
   - **Name**：`BARK_KEY`（必须一字不差，全大写，中间是下划线）
   - **Secret**：粘贴第 3 步那段密钥
5. 点 `Add secret`

存进去之后连你自己都看不到明文了，这是正常的。

---

## 第 5 步 · 生成 GitHub 令牌

这个令牌是给定时任务用的，让它有权限"戳"一下 GitHub 让程序跑起来。

1. 点右上角**你的头像** → `Settings`（这次是**账号**设置，不是仓库设置）
2. 左侧栏拉到**最底下** → **`Developer settings`**
3. **`Personal access tokens`** → **`Tokens (classic)`**
4. 右上角 `Generate new token` → 选 **`Generate new token (classic)`**
5. 填：
   - **Note**：`cron-job`（备注而已，随便写）
   - **Expiration**：选 **`No expiration`**（选了有效期，到期后推送会悄无声息地停掉）
   - **Select scopes**：只勾 **`public_repo`**（在 `repo` 大类下面缩进的那一条）
6. 拉到最底下点 `Generate token`
7. 页面顶部会出现一串 `ghp_` 开头的字符 —— **立刻复制存好**

> **这串字符只显示这一次**，刷新页面就再也看不到了。丢了只能重新生成。
> 同样地，**不要发给任何人，包括我。**

---

## 第 6 步 · 设置定时任务

1. 打开 https://cron-job.org ，注册一个账号（免费）
2. 先设时区：右上角头像 → `Settings` → **`Timezone`** 选 **`Asia/Shanghai`** → 保存
   （不设的话默认是欧洲时间，会在错误的时间触发）
3. 回首页 → **`Create cronjob`**
4. **`COMMON`** 标签页：
   - **Title**：`年线轮动`
   - **URL**：
     ```
     https://api.github.com/repos/你的用户名/yearline-rotation/dispatches
     ```
     （把"你的用户名"换成真的）
   - **Execution schedule**：选 `Custom`
     - **Days of month**：全选（或选 `Every day`）
     - **Days of week**：只勾 **周一 ~ 周五**
     - **Hours**：只勾 **21**
     - **Minutes**：只勾 **0**
5. 切到 **`ADVANCED`** 标签页：
   - **Request method**：改成 **`POST`**
   - **Headers**：点 `Add header`，加**四条**：

     | Key | Value |
     |---|---|
     | `Accept` | `application/vnd.github+json` |
     | `Authorization` | `Bearer ghp_你第5步复制的令牌` |
     | `Content-Type` | `application/json` |
     | `User-Agent` | `cron-job` |

     > `Bearer` 后面有一个空格，然后才是令牌。`User-Agent` 这条不能省，GitHub 会拒绝没有它的请求。

   - **Request body**：粘贴
     ```json
     {"event_type":"daily-update"}
     ```
6. 点 `CREATE`

法定节假日不用管——程序自己判断是不是交易日，非交易日会静默。

---

## 第 7 步 · 验证

### 先手动跑一次

1. 仓库页面 → 上方 **`Actions`** 标签
2. 左侧点 **`每日更新`**
3. 右侧 `Run workflow` → 再点绿色的 `Run workflow`
4. 等 1～2 分钟，刷新

**绿色对勾** = 成功。点进去看日志，应该有一串 `OK xueqiu...` `OK csindex...`。

**红色叉** = 失败。点进去截图给我，我来改。

> 今天如果不是交易日，程序会静默不推送，这是正常的，不代表失败。

### 再测定时任务

回 cron-job.org，找到刚建的任务，点 **`TEST RUN`**。
返回 **204** 就是成功（GitHub 的 dispatches 接口成功时就是返回 204，没有内容）。

---

## 出问题怎么办

| 现象 | 原因 | 怎么办 |
|---|---|---|
| Actions 红叉，日志里全是 `FAIL` | GitHub 服务器在美国，被中国的数据接口限流了 | 截图给我，改用别的部署方式 |
| 收到「⚠️ 数据异常」推送 | 同上，或接口改版 | 网页还能正常看，只是数据不再更新。把 Actions 日志给我 |
| 完全收不到推送 | 令牌错了 / 时区没设 / BARK_KEY 名字打错 | 按第 4、5、6 步逐条核对 |
| 网页打不开 | Pages 还没生效 | 等 5 分钟再试；仍不行检查第 2 步的 Branch 选对没 |
| 网页数字不对 | 起算日或本金设错 | 网页最底部「设置」里改，或改 `config.json` |
| 推送里没有链接 | `config.json` 的 `page_url` 还是空的 | 见下方 |

### 补上网页链接

第 2 步拿到网址后，用记事本打开 `F:\投资\创业板+红利轮动\repo\config.json`，把这一行：

```json
"page_url": "",
```

改成（换成你的真实网址）：

```json
"page_url": "https://你的用户名.github.io/yearline-rotation/",
```

保存后回 GitHub Desktop，左下角写一句说明，点 `Commit to main`，再点 `Push origin`。

---

## 安卓用户

Bark 只有 iOS 版。安卓可以换成 **PushDeer** 或 **Server 酱**，推送接口不同，代码要改几行——需要的话告诉我。

---

## 日常使用

配好之后你不需要做任何事。每个交易日 21:00：

- **没动作** → 收到一条普通推送，报当前净值和距离下一个触发点还有多远
- **有动作** → 收到高优先级推送，写明买什么卖什么、什么时候执行
- **数据出问题** → 收到告警

打开网页，顶部横幅会显示需要执行的操作，点「我已完成操作」它才消失。
**这个按钮只是提醒你别忘了下单，不影响系统计算——系统永远按 T+1 收盘自动计入，不管你点没点。**
